# Sota & Momoka's Wedding Photo Gallery

## 目的

結婚式ゲストが共有URLからアクセスし、写真・動画をアップロード、閲覧、ダウンロードできるWebアプリ。

## 現在の仕様

- 単一イベント用。アカウント登録・ログイン・イベント作成・管理画面はない。
- イベントキーを含む `/e/[eventKey]` を開く。トップは `NEXT_PUBLIC_EVENT_KEY` が設定されている場合も同じギャラリーを表示する。QRコードの生成はアプリ外で行う。
- 写真・動画は匿名で複数選択してアップロードできる。すべてマルチパートアップロードを使い、ファイルごとの進捗と失敗を表示する。キャンセル・再試行はない。
- 投稿の承認、コメント、削除はできない。
- 一覧は「すべて / 写真 / 動画」で絞り込み、S3上の更新日時が新しい順に表示する。
- 写真・動画ともカードを押すと拡大表示する。動画は拡大表示内で再生する。
- 動画は変換時に先頭フレームのサムネイルを生成し、一覧カードに表示する。
- ダウンロード選択で1件を選ぶと個別ダウンロード、2件以上を選ぶとZIPを作成してダウンロードする。「すべてをダウンロード」機能はない。
- 表示名は `Sota & Momoka's Wedding Photo Gallery`。
- 日本語、白基調、スマホ・PC対応のレスポンシブUI。
- 保存期限・動画時間・アプリ上のファイルサイズ制限は設けない。

## 構成

```text
Browser
  |-- Vercel / Next.js: 画面、一覧API、署名付きアップロードURL発行
  |-- S3 uploads/: 原本をマルチパートでアップロード（非公開）
  |-- S3 ready/: 変換済み写真・動画
  |-- S3 thumbnails/: 一覧用サムネイル
  `-- S3 archives/: ZIP

S3 uploads/ の作成イベント
  |-- 画像: Lambda（Sharp）がJPEGとサムネイルを生成
  `-- 動画: MediaConvertがMP4（H.264/AAC）と先頭フレームのJPEGを生成

ZIP作成依頼
  `-- Fargateが ready/ の選択ファイルからZIPを生成し、archives/ に保存
```

- 一覧はS3の `ready/` を直接読み取る。DBとメタデータJSONは使わない。
- `uploads/` は非公開。`ready/`、`thumbnails/`、`archives/` は読み取り公開する。
- S3イベントは `uploads/` の作成時だけProcessor Lambdaを起動し、変換結果は再処理しない。
- 画像はJPEGへ変換し、幅600px以下のJPEGサムネイルを生成する。
- 動画はMP4へ変換し、`thumbnails/` に先頭フレームのJPEGを生成する。
- 変換完了待ち・変換失敗の状態は画面に表示しない。アップロード完了後に一覧を更新し、15秒後に再度更新する。

## 画面

### ギャラリー

- タイトル、案内、写真・動画追加
- アップロード中のファイル名・進捗、失敗メッセージ
- 「すべて / 写真 / 動画」フィルターと更新ボタン
- 写真・動画の一覧、動画サムネイル、拡大表示、左右移動、スマホのスワイプ移動
- ダウンロード選択、選択解除、全選択、選択したメディアのダウンロード
- ZIP作成中の表示

## 外部設定

### AWS

- CDKでS3、Processor Lambda、MediaConvert用IAMロール、ZIP用Fargateを作成する。
- Vercel用IAMユーザーには、S3の一覧・アップロード・ダウンロードとFargate実行権限を付与する。
- Vercelに `S3_BUCKET`、`AWS_REGION`、AWS認証情報、`EVENT_KEY`、`NEXT_PUBLIC_EVENT_KEY` を設定する。
- ZIP機能には `ZIP_CLUSTER_ARN`、`ZIP_TASK_DEFINITION_ARN`、`ZIP_SUBNET_IDS`、`ZIP_SECURITY_GROUP_IDS` の設定も必要。

### 配布前

- 共有URLをQRコード化して配布する。
- iPhone、Android、PCでアップロード・一覧・動画再生・ダウンロードを確認する。
- AWSの請求アラートは必要に応じて別途設定する。

## 対象外

- アカウント、ログイン、管理画面
- 投稿承認、コメント、削除
- 複数イベント、招待者管理、パスワード
- アップロードのキャンセル・再試行
- 撮影日時の保存・表示
- メディア変換の進行状況・失敗表示
