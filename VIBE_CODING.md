# Weddind Photo System for Sota and Momoka

## 目的

結婚式ゲストがQRコードからアクセスし、写真・動画をアップロード、閲覧、個別ダウンロードできるWebアプリ。

## 決定済み要件

- 単一の結婚式専用。イベント作成・ユーザー登録・ログインは作らない。
- 共有URLは推測困難なランダム文字列を含めたQRコードにする。パスワードは付けない。
- ゲストは匿名で、写真・動画を複数選択してアップロードできる。
- アップロード中はファイルごとの進捗、完了・失敗、キャンセル、失敗分の再試行を表示する。
- 投稿承認、コメント、削除UIは作らない。
- 一覧は「すべて / 写真 / 動画」で絞り込み、撮影日時の新しい順に表示する。
- 写真は拡大表示、動画はページ内再生、各ファイルは個別ダウンロード可能にする。
- ギャラリーで複数選択できる。20件未満は個別ダウンロード、20件以上はZIPで一括ダウンロードする。20件は、一般的な回線で5MB程度の写真を約30秒で取得できる目安とする。
- 「すべてをダウンロード」はZIPで一括ダウンロードする。
- 表示名は `Weddind Photo System for Sota and Momoka`。
- 日本語、白基調のシンプルで上品なデザイン。スマホ優先・PC対応のレスポンシブUI。
- 保存期限・動画時間・アプリ上のファイルサイズ制限は設けない。S3の整理は必要時に手動で行う。

## 採用構成

```text
Browser
  |-- Vercel / Next.js: 画面、一覧API、アップロード用URL発行
  |-- S3 uploads/: 原本を直接アップロード（非公開）
  `-- S3 ready/: 変換済みメディアを表示・ダウンロード（公開）

S3 uploads/ の作成イベント
  |-- 画像: Lambdaでブラウザ表示用の画像を生成
  `-- 動画: MediaConvertでMP4（H.264/AAC）を生成
                 `-- S3 ready/ へ出力

ZIP作成依頼
  `-- Fargateが ready/ からZIPを生成
                 `-- S3 archives/ へ出力
```

- フロントエンドは Next.js + TypeScript をVercelへ配置する。
- メディア一覧用のDBは使わない。`ready/` 内のメディアと付随メタデータJSONをVercelのAPIが読み取る。
- `uploads/` は原本保管用。ゲストへは公開しない。
- `ready/` のみ読み取り公開する。書き込みは公開しない。
- S3の直接アップロードは、Vercel APIが発行する短時間有効な署名付きURLを使う。大容量動画はマルチパートアップロードを使う。
- 一覧・ダウンロードは変換済みファイルのみを使う。原本はS3内に保持する。
- S3イベントの入力対象を `uploads/` に限定し、変換結果の `ready/` を再処理しない。
- ZIPはVercelやLambdaでは生成しない。FargateがS3から生成して `archives/` に保存する。

## メディア変換

- HEIC/HEIFを含む画像は、ブラウザ表示用のJPEGを生成する。
- すべての動画は、MP4（H.264/AAC）へ変換・圧縮する。
- 原本にある撮影日時などのメタデータは、変換後にも引き継ぐ。
- 変換済みメディアごとに、撮影日時・種別・変換状態を記録したJSONを `ready/` に置く。
- 変換が終わるまではアップロード画面で処理中を表示する。変換失敗時は失敗を表示する。

## 画面

### トップ

- タイトル
- 「写真・動画を追加」
- 「ギャラリーを見る」

### アップロード

- 複数ファイル選択
- ファイルごとの進捗率、キャンセル、再試行、完了・失敗表示

### ギャラリー

- 「すべて / 写真 / 動画」フィルター
- 撮影日時の新しい順
- 写真の拡大表示
- 動画のインライン再生
- 各メディアの個別ダウンロード
- 複数選択。20件以上の選択時はZIP一括ダウンロード
- 「すべてをダウンロード」のZIP一括ダウンロード

## 実装順

1. Next.jsの画面とレスポンシブUIを作る。
2. 共有URL、一覧API、S3署名付きアップロードを実装する。
3. S3の `uploads/` / `ready/` と公開範囲を設定する。
4. 画像変換Lambdaと動画変換MediaConvertを接続する。
5. ZIP生成用のFargateと、複数選択・全件ダウンロードを実装する。
6. 変換待ち・失敗・複数アップロードのUIを実装する。
7. iPhone、Android、PCでアップロード・表示・再生・個別・ZIPダウンロードを確認する。

## アプリで作ること

- QRコードの共有URLで開くトップ、アップロード、ギャラリー画面
- スマホ・PC向けのレスポンシブ表示
- 複数ファイル選択、進捗、キャンセル、再試行
- S3への署名付きアップロードURLを発行するAPI
- S3の `ready/` を読む一覧API、写真・動画フィルター、撮影日時順の表示
- 写真の拡大、動画再生、個別ダウンロード
- 複数選択・全件ZIPの作成依頼、ZIP準備中・完了画面
- 変換待ち・変換失敗の表示

## アプリ外で設定・用意すること

### AWS

- AWSアカウント、支払い設定、予算アラート
- S3バケットと `uploads/`、`ready/`、`archives/` の作成
- `ready/` と `archives/` の読み取り公開、`uploads/` の非公開設定
- S3 CORS設定と、アップロード・変換・ZIP作成用のIAM権限
- 画像変換用Lambdaのデプロイ
- 動画変換用MediaConvertのキュー・IAMロール・出力先設定
- S3アップロードイベントから画像変換・動画変換を起動する設定
- ZIP作成用Fargateのコンテナイメージ、タスク定義、実行権限の設定
- CloudWatchログの確認先を用意

### Vercel

- Vercelプロジェクトの作成とデプロイ
- AWSの認証情報、S3バケット名、共有URL用シークレットを環境変数へ設定
- VercelのURLを共有URLとして使う。独自ドメインは必須ではない。

### 結婚式前

- 共有URLのQRコードを画像化し、招待状・会場案内へ掲載
- iPhone、Android、PCで実機確認
- AWSの請求アラートが届くことを確認

## 対象外

- アカウント、ログイン、管理画面
- 投稿承認、コメント、削除UI
- 複数イベント、招待者管理、パスワード
